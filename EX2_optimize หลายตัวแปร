ตัวอย่างที่ 2: มี 2 ตัวแปร
หาค่า x1, x2 ที่ทำให้ f = (x1-2)² + (x2-5)² น้อยที่สุด


fun = @(x) (x(1)-2)^2 + (x(2)-5)^2;

x0 = [0, 0];  % เริ่มจาก x1=0, x2=0

x_opt = fminunc(fun, x0);

disp(['x1 = ', num2str(x_opt(1))])  % ควรได้ 2
disp(['x2 = ', num2str(x_opt(2))])  % ควรได้ 5
